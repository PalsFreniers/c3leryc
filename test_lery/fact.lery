import std::io;

fn entry() -> void {
        in: i32;
        io::getI32(in);
        io::printfn("%d", fact(in));
}

fn fact(x: i32) -> i32 {
        i: i32;
        while(x != 0) {
                i *= x;
                x--;
        }
        return i;
}
